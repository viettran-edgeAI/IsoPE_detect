{
    "input_dir": {
        "value": "development_phase/data/optimized",
        "description": "Directory containing split CSV files. Expected names: <model_name>_ben_train.csv, <model_name>_ben_val.csv, <model_name>_mal_val.csv, <model_name>_ben_test.csv, <model_name>_mal_test.csv."
    },
    "optimized_features_json": {
        "value": "",
        "description": "Optional path to optimized feature list JSON; if empty, tool auto-uses <output_dir>/<model_name>_optimized_features.json when present."
    },
    "output_dir": {
        "value": "embedded_phase/core/models/isolation_forest/resources",
        "description": "Directory where all resulting files are written."
    },
    "model_name": {
        "value": "iforest",
        "description": "Base model name for generated artifacts."
    },
    "quantization_bits": {
        "value": 4,
        "description": "Bits per feature for quantization (1-8)."
    },
    "quantization_tool": {
        "value": "tools/data_quantization/processing_data",
        "description": "Path to the quantization executable to invoke."
    },
    "header": {
        "value": "auto",
        "options": ["auto", "yes", "no"],
        "description": "CSV header handling mode passed to quantization tool."
    },
    "problem_type": {
        "value": "isolation",
        "options": ["isolation", "classification", "regression"],
        "description": "Problem type passed to quantization tool."
    },
    "remove_outliers": {
        "value": false,
        "options": [true, false],
        "description": "Whether to enable outlier clipping in quantization preprocessing."
    },
    "dev_optimized_config_dir": {
        "value": "",
        "description": "Directory containing optimized JSON config files to copy into output_dir for model components: feature_list, scaler, feature_transform, model_config"
    }
}
